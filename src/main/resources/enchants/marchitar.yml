# ============================================================
#  enchants/marchitar.yml — Marchitar
# ============================================================

id: marchitar
enabled: true

display-name: "Marchitar"
tier: comun
max-level: 2
icon: WITHER_ROSE

applicable-to:
  - SWORD
  - AXE

conflicts: []

lore-line: "<gray>Marchitar {nivel_romano}"

description:
  - "<gray>Probabilidad de marchitar a tu objetivo."
  - "<dark_gray>│ <gray>Probabilidad: <white>10% + 5%</white> <gray>por nivel"
  - "<dark_gray>│ <gray>Duración: <white>2s + 1s</white> <gray>por nivel"

fabled-skill: ""
sync-level-with-skill: false

# --- Efecto nativo (motor propio: no depende de Fabled) ---
effects:
  trigger: attack
  chance-base: 10
  chance-scale: 5
  actions:
    - type: potion
      target: victim
      value: Wither
      amplifier: 0
      seconds-base: 2
      seconds-scale: 1
