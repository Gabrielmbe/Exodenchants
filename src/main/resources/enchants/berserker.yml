# ============================================================
#  enchants/berserker.yml — Berserker
#  Tier MITICO · nivel máximo 4
# ============================================================

id: berserker
enabled: true

display-name: "Berserker"
tier: mitico
max-level: 4
icon: BREEZE_ROD

applicable-to:
  - AXE

conflicts: []

lore-line: "<gradient:#F72585:#B5179E>Berserker {nivel_romano}</gradient>"

description:
  - "<gray>Al matar entras en frenesí: fuerza,"
  - "<gray>velocidad y resistencia a la vez."
  - "<dark_gray>│ <gray>Duración: <white>6s + 2s</white> <gray>por nivel"

fabled-skill: "FE_Berserker"
sync-level-with-skill: true

# --- Efecto nativo (motor propio: no depende de Fabled) ---
effects:
  trigger: kill
  chance-base: 100
  chance-scale: 0
  actions:
    - type: potion
      target: self
      value: Strength
      amplifier: 1
      seconds-base: 6
      seconds-scale: 2
    - type: potion
      target: self
      value: Speed
      amplifier: 1
      seconds-base: 6
      seconds-scale: 2
    - type: potion
      target: self
      value: Resistance
      amplifier: 0
      seconds-base: 6
      seconds-scale: 2
    - type: particle
      target: self
      value: Flame
      count: 15
