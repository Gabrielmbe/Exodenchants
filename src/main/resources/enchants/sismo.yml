# ============================================================
#  enchants/sismo.yml — Sismo
# ============================================================

id: sismo
enabled: true

display-name: "Sismo"
tier: legendario
max-level: 3
icon: NETHERITE_AXE

applicable-to:
  - AXE
  - MACE

conflicts: []

lore-line: "<gradient:#FFD166:#FFB703>Sismo {nivel_romano}</gradient>"

description:
  - "<gray>Probabilidad de repeler al objetivo con"
  - "<gray>una onda de choque."
  - "<dark_gray>│ <gray>Probabilidad: <white>10% + 5%</white> <gray>por nivel"

fabled-skill: "FE_Sismo"
sync-level-with-skill: true

# --- Efecto nativo (motor propio: no depende de Fabled) ---
effects:
  trigger: attack
  chance-base: 10
  chance-scale: 5
  actions:
    - type: push
      target: victim
      value-base: 2
      value-scale: 0.5
    - type: damage
      target: victim
      value-base: 2
      value-scale: 1.5
    - type: particle
      target: victim
      value: Explode
      count: 15
    - type: sound
      target: victim
      value: ENTITY_GENERIC_EXPLODE
      volume: 0.6
      pitch: 1.1
