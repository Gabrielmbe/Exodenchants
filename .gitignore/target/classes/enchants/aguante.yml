# ============================================================
#  enchants/aguante.yml — Aguante
# ============================================================

id: aguante
enabled: true

display-name: "Aguante"
tier: comun
max-level: 2
icon: IRON_LEGGINGS

applicable-to:
  - LEGGINGS

conflicts: []

lore-line: "<gray>Aguante {nivel_romano}"

description:
  - "<gray>Absorbes parte del golpe recibido."
  - "<dark_gray>│ <gray>Duración: <white>4s + 2s</white> <gray>por nivel"

fabled-skill: "FE_Aguante"
sync-level-with-skill: true

# --- Efecto nativo (motor propio: no depende de Fabled) ---
effects:
  trigger: defend
  chance-base: 100
  chance-scale: 0
  actions:
    - type: potion
      target: self
      value: Absorption
      amplifier: 0
      seconds-base: 4
      seconds-scale: 2
