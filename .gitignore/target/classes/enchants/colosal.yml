# ============================================================
#  enchants/colosal.yml — Probabilidad de golpe devastador
# ============================================================

id: colosal
enabled: true

display-name: "Colosal"
tier: legendario
max-level: 3
icon: ANVIL

applicable-to:
  - SWORD
  - AXE
  - MACE

conflicts: []

lore-line: "<gradient:#FFD166:#FFB703>Colosal {nivel_romano}</gradient>"

description:
  - "<gray>Probabilidad de asestar un golpe"
  - "<gray>devastador con daño adicional."
  - "<dark_gray>│ <gray>Probabilidad: <white>10% + 5%</white> <gray>por nivel"
  - "<dark_gray>│ <gray>Daño extra: <white>3.0 + 2.0</white> <gray>por nivel"

fabled-skill: "FE_Colosal"
sync-level-with-skill: true

# --- Efecto nativo (motor propio: no depende de Fabled) ---
effects:
  trigger: attack
  chance-base: 10
  chance-scale: 5
  actions:
    - type: damage
      target: victim
      value-base: 3
      value-scale: 2
    - type: sound
      target: victim
      value: ENTITY_PLAYER_ATTACK_CRIT
      volume: 1
      pitch: 0.8
